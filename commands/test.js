// Importación de dependencias.
const {RichEmbed} = require('discord.js');

// creación del comando.
module.exports = async (client, message, arguments) => {
    message.reply("esto es un comando para testeo... Vuelva pronto.");
};

